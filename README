"Switch to Tab" allows you to quickly search your open tabs and switch between them using the omnibox.

Enter "sw<tab>" followed by a search term to begin using Switch to Tab. You can enter multiple space-separated keywords.

Shortcut: If you press enter without selecting a match, the extension will select the first one (if available).
